public class VoidRenderer implements Renderer {
    VoidRenderer() {}

    /**
     * Does not print anything when method is called.
     * @param board the board to print.
     */
    public void renderBoard(Board board) {
    }
}
